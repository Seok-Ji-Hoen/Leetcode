class Solution:
    def isPalindrome(self, x: int) -> bool:
        
        word = str(x)
        
        for i in range(len(word)):
            if word[i]!=word[-i-1]:
                return False
            
        
        return True
